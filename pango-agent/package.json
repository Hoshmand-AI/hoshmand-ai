{
  "name": "pango-agent",
  "private": true,
  "version": "1.0.0",
  "description": "Calls you relentlessly (caller: Pango) when you leave your parents' house so you remember to stop your Pango parking session.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --outDir .test-dist --noEmit false --module commonjs --target ES2022 --esModuleInterop --skipLibCheck api/*.ts lib/*.ts && node test/smoke.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "twilio": "^5.3.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vercel/node": "^3.2.24",
    "typescript": "^5.5.4"
  }
}
